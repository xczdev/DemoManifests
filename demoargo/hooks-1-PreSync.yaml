apiVersion: batch/v1
kind: Job
metadata:
  name: app-notification-presync
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      containers:
      - name: echo-notification-presync
        image: alpine
        command:
          - "echo L’application sera synchronisé avec un nouvel état désiré."
      restartPolicy: Never
  backoffLimit: 2