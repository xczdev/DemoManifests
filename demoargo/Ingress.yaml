apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress
  annotations:
    demo-service.formation.argocd.local/rewrite-gartget: /
spec:
  rules:
    -http:
      paths:
        - path: /
        pathType: Prefix
        backend:
          service:
            name: ajoutIngress
            port:
              number: 90
